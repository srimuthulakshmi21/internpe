import java.util.Random;
import java.util.Scanner;

public class RockPaperScissors {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        Random random = new Random();

        String[] choices = {"rock", "paper", "scissors"};
        String userChoice, computerChoice;
        String playAgain;

        System.out.println("Welcome to Rock Paper Scissors!");

        do {
            // 🔹 Ask user input until valid
            while (true) {
                System.out.print("Enter your choice (rock, paper, scissors): ");
                userChoice = sc.next().toLowerCase();

                if (userChoice.equals("rock") || userChoice.equals("paper") || userChoice.equals("scissors")) {
                    break; // valid → exit this loop
                } else {
                    System.out.println("Invalid choice! Please enter rock, paper, or scissors only.\n");
                }
            }

            // 🔹 Computer’s random choice
            int randomIndex = random.nextInt(3);
            computerChoice = choices[randomIndex];
            System.out.println("Computer chose: " + computerChoice);

            // 🔹 Compare results
            if (userChoice.equals(computerChoice)) {
                System.out.println("It's a draw!");
            } else if ((userChoice.equals("rock") && computerChoice.equals("scissors")) ||
                       (userChoice.equals("paper") && computerChoice.equals("rock")) ||
                       (userChoice.equals("scissors") && computerChoice.equals("paper"))) {
                System.out.println("You win!");
            } else {
                System.out.println("Computer wins!");
            }

            
            while (true) {
                System.out.print("Do you want to play again? (yes/no): ");
                playAgain = sc.next().toLowerCase();

                if (playAgain.equals("yes") || playAgain.equals("no")) {
                    break; // valid input → exit this inner loop
                } else {
                    System.out.println("Invalid input! Please enter 'yes' or 'no' only.\n");
                }
            }

        } while (playAgain.equals("yes"));

        System.out.println("Thanks for playing!");
        sc.close();
    }
}
